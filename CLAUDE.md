# Arcade Ruins — Project Context

**Read this file and `STATE.md` before doing anything else.** `STATE.md` is the live status;
this file is the standing rules.

## What this project is

Re-engineering [AudioKit Synth One](https://github.com/AudioKit/AudioKitSynthOne) (iOS/iPadOS,
archived 2022, v1.4.1) into a macOS product shipped as **both** a standalone app and an **AUv3
instrument plugin** (`aumu` / `ruin` / `BP03`) for Logic, Live, GarageBand, Reaper and Bitwig.

**Since 2026-09-21 (ADR-092) the Mac products are named "Arcade Ruins Classic" and the JUCE plugin is "Arcade Ruins"** — the name only; every code, identifier and file name below stands.

**The product is "Arcade Ruins"** (P5-4, ADR-029) — an unofficial port, MIT, not affiliated with
AudioKit. Internal target and module names are deliberately still `SynthOne*`: they are a
provenance signal (ADR-009) and invisible to users. Rebrand at the product-identity level only.
**`Scripts/branding/generate.py` regenerates every branded asset.** Both the wordmark and the
icon are the owner's artwork — `Scripts/branding/source/Arcade-Ruins.png` fitted to each frame
(ADR-029), and `Arcade-Ruins-Icon.png` cropped into `Sources/SynthOne/ArcadeRuins.icon`, the
Icon Composer bundle the app ships (ADR-052; a legacy icon set is composited inside macOS 26's
own shape, which nests one rounded square in another). `appicon.py`, the code-drawn icon, no
longer ships anything.

**Hard requirements from the owner:**
1. **Preserve the user interface** — 12 storyboard panels, custom knobs, touch pads, keyboard.
   **Relaxed 2026-09-12 (ADR-045, Phase 6):** the owner chose a desktop redesign. The classic
   interface is kept — tag `v0.1.0-classic-ui`, and in the build behind `S1ClassicLayout` — and the
   desktop layout re-homes the *same* storyboard controls, so the panels, knobs and bindings are
   still the source of truth. See `Sources/SynthOneCore/Desktop/`. Since then: skins over that layout
   (ADR-046, Phase 7 — a skin is a palette and decoration, never a layout; ADR-048 — a skin may
   give each section its own accent, which every control inside draws in) and layout revisions the
   owner asks for one at a time (ADR-047, Phase 8). The classic layout gets none of this.
2. **Preserve functionality** — same DSP, same 150 parameters, same presets and tunings.
3. Multi-session effort; every session must leave the project resumable.

**Settled:** Mac Catalyst (ADR-001, spike-proven) · no AudioKit dependency (ADR-002) · XcodeGen
(ADR-003) · no Ableton Link (ADR-004) · local-only (ADR-005); the app and plugin are **signed with the
owner's team 8RSH7U3222** so they share presets through an App Group (ADR-041, which supersedes ADR-005's
ad-hoc signing) ·
named Arcade Ruins, AU subtype `ruin` (ADR-029).

## Start here, every session

```bash
cat STATE.md                 # current phase, task board, next action
git log --oneline -10
Scripts/fetch-references.sh  # if .references/ is missing (it is gitignored)
Scripts/build.sh             # regenerate + signed build (team 8RSH7U3222, ADR-041)
# Tests build unsigned, so they need no certificate. Keep them out of ./DerivedData,
# which is what gets installed (ADR-038):
xcodebuild -project SynthOne.xcodeproj -scheme SynthOne \
    -destination 'platform=macOS,variant=Mac Catalyst' -derivedDataPath /tmp/SynthOneTestDD \
    CODE_SIGNING_ALLOWED=NO test
Scripts/validate-linux.sh    # Linux in Docker: build, tests, validators, RealtimeSanitizer (ADR-079)
Scripts/validate-plugin.sh   # the JUCE VST3: Steinberg's validator + pluginval strictness 10 (ADR-079)
Scripts/validate-au.sh       # auval — a real check since P4-2. ~8s since P4-3; a hang is at 0% CPU
# The portable engine (cross-platform plan, X1). SDKROOT first, or CMake cannot link (see gotchas):
export SDKROOT=$(xcrun --sdk macosx --show-sdk-path)
cmake -S . -B build/engine && cmake --build build/engine && ctest --test-dir build/engine --output-on-failure   # incl. the 20 goldens through the engine alone (ADR-071)
# The desktop layout (Phases 6–8): render the DerivedData app in-process and look at the PNG.
DRIVER_OUT=/tmp/arcade-ruins-debug RENDER_TAG=desktop RENDER_SELECT="Brice Beasley,0: " \
    xcrun lldb -b -o "command script import Scripts/debug/desktop_render.py"
# RENDER_ARGS="-S1Skin cabinet" renders a skin; RENDER_PRESS=Presets opens the browser.
# Its suites: -only-testing:SynthOneTests/DesktopLayoutTests, SkinTests, DesktopPluginTests.
```

**Run the tests often.** Since P2-4 the suite includes golden renders of 20 shipped presets, so an
accidental change to the DSP fails immediately instead of at the end of a phase.

Then do the **Next action** named at the top of `STATE.md` — its first section, "RESUME HERE",
is the cross-platform track's live status and says how work is verified now.

**The JUCE plugin (cross-platform plan, X2 onward)** — read `Sources/S1Plugin/README.md` first:

```bash
export SDKROOT=$(xcrun --sdk macosx --show-sdk-path)             # CMake only; UNSET it for xcodebuild and Scripts/build.sh
cmake -S . -B build/plugin -DS1_BUILD_PLUGIN=ON && cmake --build build/plugin -j 10
(cd build/plugin && ctest)                                       # 43 tests: the engine's, the goldens bit-exact, the plugin's
Scripts/write-layout-spec.sh                                     # after a Mac desktop-layout change: regenerate the JUCE interface's layout (ADR-083)
Scripts/check-layout-spec.sh                                     # …and hold it to the running Mac app, with wireframe overlays
Scripts/validate-plugin.sh                                       # Steinberg's validator + pluginval strictness 10
Scripts/validate-plugin.sh --au                                  # …and the JUCE AU (aumu/ArRu/BP03): INSTALLS it for this user, auval, pluginval (ADR-093)
Scripts/validate-linux.sh                                        # the same and the RealtimeSanitizer, in Docker (Docker Desktop running)
Scripts/release-plugin.sh                                        # X4: the macOS installer — universal, Developer ID, notarised (NOTARISE=0 stops before Apple). ADR-094
Scripts/release-linux.sh                                         # X4: the Linux tarball, built on Ubuntu 22.04 in Docker (--x86: emulated, slow)
# Windows: Scripts\release-windows.ps1 on the owner's machine (Inno Setup 6) — the installer, with the C runtime linked in.
# Windows: the owner runs Scripts\validate-windows.ps1 on their machine and pastes the block back.
open "build/plugin/Sources/S1Plugin/ArcadeRuins_artefacts/Release/Standalone/Arcade Ruins.app"   # the JUCE standalone
```

There is **no CI** (ADR-079): those scripts are the verification, and a task merges when macOS and
Linux are green and STATE.md says whether Windows has been run. **Never make this repository
public.**

## Layout

```
README.md      Public-facing: what it is, how to build it, attribution
LICENSE        MIT (ours) · NOTICE.md — every upstream licence and the trademark position
PORT_PLAN.md   Phases and task IDs (P0-1 … P8-n) with acceptance criteria
STATE.md       Live status + session log. THE resume point.
docs/          01-decisions.md (ADRs) · 02-audiokit-api-surface.md · 03-parity-checklist.md
               04-build-and-test.md · reference/ (App Store screenshots)
upstream/      Pinned READ-ONLY copy of AudioKitSynthOne @ 6466a37
.references/   Pinned AudioKit 4.9.2, gitignored — fetch with Scripts/fetch-references.sh
project.yml    XcodeGen spec — the source of truth for targets and build settings
Sources/       Soundpipe/ · S1Support/ · SynthOneCore/ · SynthOne/ · SynthOneAU/
               S1Engine/  The portable C++ engine: the kernel, note state, sequencer, rate, tunings, presets,
                          wavetable loader, S1Parameter.h and S1EngineTypes.h — compiled by BOTH the Xcode
                          framework and CMake (X1, ADR-057, ADR-065–070)
               S1Plugin/  The JUCE plugin (VST3 + Standalone; X2, ADR-072). CMake only, -DS1_BUILD_PLUGIN=ON;
                          JUCE is FETCHED at configure time and never committed. Read its README.md first
CMakeLists.txt The portable build: engine + Tests/Engine (+ S1Plugin and Tests/Plugin with S1_BUILD_PLUGIN).
               Not the app or the AUv3 — those stay XcodeGen's
               SynthOneCore/Desktop/  The desktop layout, its drawing, skins and Arcade art (P6–P8)
Scripts/debug/ lldb drivers; desktop_render.py renders the running layout to a PNG
Scripts/branding/  Generates the wordmark and the 18 app icons
Tests/ Scripts/
```

Each vendored or ported directory carries its own record: `Sources/Soundpipe/VENDORING.md`,
`Sources/S1Support/PORTING.md`, `Sources/SynthOneCore/AudioUnitBase/PORTING.md`,
`Sources/SynthOneCore/DSP/PORTING.md`, `Sources/S1Engine/PORTING.md`, `Sources/S1Plugin/README.md`. **Read the relevant one before touching that code.**

## Ground rules

- **`upstream/` is read-only.** Port code *out* of it. Never edit it.
- **`SynthOne.xcodeproj` is generated.** Edit `project.yml`, run `xcodegen generate`. Hand edits are lost.
- **No CocoaPods.** Everything is vendored source.
- **Ported code keeps its `AK` names** (ADR-009). `SynthOneCore` re-exports `S1Support`, so porting a
  Synth One file means changing `import AudioKit` → `import SynthOneCore` and nothing else. The `S1`
  prefix is for code we own outright. The prefix is a provenance signal — respect it.
- **Minimal diffs against upstream.** This is a port, not a redesign. When you must change ported
  code, leave a `PORT FIX:` or `PORT:` comment saying why, and record it in that directory's
  PORTING.md.
- **Tests assert behaviour, not linkage.** A bad port compiles and links fine and sounds wrong —
  that is exactly how ADR-011 happened. Measure frequencies, amplitudes, envelope shapes.
- **Never mark a task done in `STATE.md` unless it builds and its verification step passed.**

## Session protocol

**End of session** (or when context runs low):
1. Update `STATE.md` — task board, blockers, **Next action**.
2. Append a dated Session Log entry (what changed and *why*, including anything surprising).
3. Record non-obvious decisions in `docs/01-decisions.md` as a new ADR.
4. Commit, prefixed with the task ID (`P1-3: ...`). Push.

Use a heredoc for commit messages (`git commit -F -`) — backticks in `-m` get shell-interpreted.

## Hard-won gotchas

Each of these cost real time. See `docs/04-build-and-test.md` for the build-level list.

| | |
|---|---|
| **Symbol presence ≠ compatibility** | P1-2 vendored canonical Soundpipe after checking every `sp_*` name existed. The signatures and semantics differed, and the band-limited `sp_oscmorph2d` was missing entirely — the anti-aliasing would have silently vanished. ADR-011 |
| `sp_create` returns **0**, not `SP_OK` (=1) | Only `base.c` deviates; every module's `*_create` returns `SP_OK`. Check the out-pointer |
| `pow2(x)` **squares**; it is not 2^x | It is Synth One's velocity curve. Misreading it changes how the instrument plays |
| `upstream/DSP/Kernel/oscmorph2d.c` is a **stale decoy** | The real band-limited module is in AudioKit's `Core/SoundpipeExtension/` |
| AudioKit 4.9.2 **does not compile** on modern Swift | Three integer/float narrowing bugs, incl. `exp2((noteNumber - 69) / 12)` — integer division that would collapse 128 notes onto ~11 frequencies |
| `AKAudioUnit` **replaces** `implementorValueObserver` | `allocateRenderResources` overwrites whatever `createParameters` installed. Upstream behaviour, preserved — and it is why host events arrive as *render* events at all. The observer you write in `createParameters` is not the one that runs. ADR-010, ADR-022 |
| Catalyst renders the iPad UI at **77%** | The idiom choice is an explicit P3-1 decision, not a default. ADR-007 |
| App Groups need a Team ID | Unavailable under ad-hoc signing, so P3-3 needs different shared storage. ADR-006 |
| `Bundle.main` in ported UI code is a latent **runtime** crash | Storyboards, presets and assets are framework resources. Use `Bundle.synthOneCore` / `UIImage.synthOne(_:)`. `#imageLiteral` **traps** rather than returning nil. ADR-017 |
| A storyboard class that fails to resolve is **silent** | You get a bare `UIView` with nil outlets, not an error. `UILoadTests` asserts concrete types for this reason. 18 `customModule` attributes needed correcting at P3-1 |
| `assert` is compiled out of Release | Upstream shipped a bounds assertion that reads `17 < 16`. Their Release build never saw it; every Debug build of the Tunings panel died on launch |
| XcodeGen makes **every** framework header public by default | And it keeps the **first** source entry matching a file, so per-file `public` entries must precede the directory that sweeps up the rest. Get it wrong and the framework ships no public headers *while still building*. ADR-012 |
| `AK_ENUM` is **not** `NS_ENUM` | It is `enum __attribute__((enum_extensibility(open))) a : int`. Our shim had it wrong for five sessions; it only compiled because `S1Parameter.h` was reached from Obj-C++. ADR-012 |
| `#pragma once` ≠ include guard here | Public headers are *copied*, so the same header reached from the source tree and from `<SynthOneCore/…>` is two files. Use named guards. ADR-012 |
| A note struck right after `allocateRenderResources` **sweeps up into tune** | `sp_port_init` zeroes its state, so parameters re-ramp from 0 while `S1NoteState::run` multiplies `oscmorph->freq` by `detuningMultiplier` every sample. Upstream. Do not "fix" it. ADR-013 |
| `xcodebuild` does not pass the shell env to the test runner | `SYNTHONE_WRITE_GOLDENS=1 xcodebuild …` is silently ignored; it must be `TEST_RUNNER_…`. Use `Scripts/write-goldens.sh` |
| Goldens are **float32**, not 16-bit | Four of the twenty presets render above full scale; integer PCM clamps them and the suite fails on clipping. ADR-016 |
| Never `@objc` anything in `SynthOneCore` that mentions an `S1Support` type | It lands in `SynthOneCore-Swift.h` as `@import S1Support`, which no consumer can resolve — S1Support is a static library. ADR-014 |
| A 90-second `AVAudioEngine` was a **consent dialog**, not CoreAudio | Upstream's `.playAndRecord` session prompts for the microphone and blocks until answered — the app is `.playback` now. Re-measured, `outputNode` costs 0.59 s. The rules that survive: enable manual rendering *first* when you want no hardware, and an AUv3 must **never** touch `outputNode`. ADR-015, corrected |
| Wavetables must be loaded **before** `allocateRenderResources` | It walks `ft_array` to rescale `sicvt`, and `S1NoteState::init` hands the array to `sp_oscmorph2d_init`. `destroy()` does not free them, so load once, outside the cycle |
| **A hang is not slowness.** Check CPU first | `auval` "running for 2.5 hours" was at **0.0% CPU** with no output — blocked, not busy. `ps`/`sample` answers it in one command. I guessed "slow because 150 parameters" and was wrong. ADR-021 |
| A running host holds the **old** plugin | macOS registers the AUv3 from `/Applications`, but a host that is already open does not rescan. A stale extension that crashed on load presents as **silence**, not an error — it cost a false "audio regression" on 2026-09-09. `Scripts/validate-au.sh` warns now; quit and relaunch the host after every install |
| An out-of-process AUv3 turns a **crash into a hang** | The extension segfaults, the host blocks forever on an XPC reply, and nothing in the host's output says the plugin died. **Always check `~/Library/Logs/DiagnosticReports/SynthOneAU-*.ips`**, not just the host's. ADR-021 |
| **The originator token does not cross the process boundary** | Out of process — Logic — the host sends every write the plugin's interface makes back into the extension's tree with no originator, so `token(byAddingParameterObserver:)` hears it as a host move. The in-process test passed throughout. `S1HostedSynth.echoWindow` suppresses it. Found by attaching lldb to the extension *inside Logic* (Debug builds carry `get-task-allow`) — the evidence that settled it in one run. ADR-030 |
| `scheduleParameterBlock` events never reach `internalRenderBlock` | The framework holds them; `AUAudioUnit.renderBlock` is what drains the list into `realtimeEventListHead`. Driving `internalRenderBlock` directly in a test delivers **no** scheduled parameters, and looks exactly like `startRamp` being a no-op. ADR-022 |
| `dependentParameters:nil` is not a safe default | `arpRate`/`tempoSyncToArpRate` re-drive four other parameters, so one host automation move changes five DSP values. A host that is not told keeps showing — and writing back — stale ones. ADR-022 |
| Localized `InfoPlist.strings` **override** `Info.plist` | Six of them hard-coded `CFBundleDisplayName = "Synth One"`. Renaming the product in `project.yml` alone leaves the old name in those languages. ADR-029 |
| The AU's `manufacturer`+`subtype` is **permanent** | A host stores that pair in its session, not the name. `aks1` → `ruin` was free only because nothing had shipped. ADR-029 |
| Ad-hoc builds are **rejected by Gatekeeper** | `spctl -a` says `rejected` even locally, and a downloaded copy fails as "damaged" with no dialog to click through. Public binaries need P5-2, or a documented `xattr -dr com.apple.quarantine` step. STATE.md |
| Mac-style controls **cannot be rendered in a test bundle** | `drawHierarchy` on a `.mac`-style control throws `NSInternalInconsistencyException` — there is no `NSApplication`. Assert `preferredBehavioralStyle`, and get the owner's eyes on the pixels. ADR-029 |
| **Listing button classes misses buttons** | ADR-026 fixed six classes; plain storyboard `UIButton`s with no `customClass` kept macOS chrome until a hierarchy walk found five more. Walk the view tree instead. ADR-029 |
| Signed `AUEventSampleTime` cast to unsigned `AUAudioFrameCount` | `eventSampleTime - now` is negative for `AUEventSampleTimeImmediate` — i.e. for *every* host automation move — and the cast makes it ~4 billion frames. Apple sample code, vendored by AudioKit, never executed in Synth One's history because the AU parameter tree was never implemented. ADR-021 |
| **The plugin must never open MIDI inputs of its own** | It did, through `Manager`'s CoreMIDI listener. In Logic one key played twice — as two different notes, on two threads — and a plugin on an unselected track answered a hardware keyboard. Host MIDI arrives in the render block and goes through `S1HostMIDI`, a render-thread port of `Manager` → `KeyboardView` → `SDSustainer`. **Change one, change the other:** `HostMIDIParityTests` compares them note for note. ADR-031 |
| **`x.take()` on an implicitly unwrapped optional is `Optional.take()`** | Swift type-checks an IUO as `Optional` first, and since 5.9 `Optional` has `take()`, which returns the value *and sets the property to nil*. A test recorder with its own `take()` emptied itself on first use and crashed one call later, far from the cause. Never name a method `take` on anything held in an IUO. ADR-031 |
| **A test run that executes 0 tests succeeds** | `xcodebuild test -only-testing:…` with a filter that matches nothing reports `TEST SUCCEEDED`, `Executed 0 tests`. A revert-and-fail check built that way proves nothing. The shell here is **zsh**, which does not word-split an unquoted `$VAR`, so `-only-testing` flags gathered into one variable became one argument that matched nothing — and a new test file added after `xcodegen generate` ran does the same. Write the flags out literally, and check `Executed N tests` with N > 0. ADR-032 |
| **Some UIKit controls throw under the Mac idiom, and only once they enter a window** | `UIPickerView` crashed the preset editor in both products. Loading it in the `.pad` test bundle is fine, so nothing caught it. UIKit's restricted classes are the ones with a `UICatalystMacIdiomUnsupported_Internal` category: `UIButton`, `UIPickerView`, `UIRefreshControl`, `UISlider`, `UIStepper`, `UISwitch`. `MacIdiomControlTests` walks every storyboard scene for them. **Driving the app from lldb Objective-C expressions has three traps.** Never call a variadic method such as `stringWithFormat:`: with no prototype, its arguments land in the wrong place. A breakpoint callback does not run inside an expression, because lldb's own exception breakpoint interrupts the expression first. And give every local a unique prefix: names like `table`, `S` or `context` collide with symbols in loaded images, and the expression does not compile. ADR-033 |
| **Dark appearance turns unset text colours white** | A storyboard control with a fixed light background and no text colour draws in `labelColor`, which is white in Dark. The preset and bank editors' name fields were white on `#F8F8F8`. Upstream had the same bug on a Dark-mode iPad. Pin such a control with `overrideUserInterfaceStyle = .light`, not the app: the rest of the design is only right in Dark. **To measure the running app, inject a library rather than typing lldb expressions**: the Debug build has no hardened runtime, so `open -g --env DYLD_INSERT_LIBRARIES=…` works. See `Scripts/debug/appearance_walker.m`. **In the test bundle an appearance override is not seen until a trait update**: with no window nothing triggers one, and an override set in `viewDidLoad` was invisible, so the test failed identically with and without the fix. Lay out and call `updateTraitsIfNeeded()`. ADR-034 |
| **A window-less view does not lay out after a constraint change** | Setting a constraint's `constant` and calling `layoutIfNeeded()` left the keyboard where it was in the test bundle. Call `setNeedsLayout()` first. The first shown-keyboard test failed that way with the layout correct; a diagnostic that printed frames settled it. ADR-035 |
| **The test bundle must never write the owner's files** | `Disk` resolves the real `~/Library/Application Support/SynthOne/` and `~/Library/Caches`, and the test bundle is not sandboxed. Until ADR-036, a test that fired the keyboard toggle saved a fresh `AppSettings` over the owner's `settings.json`, and `tunings_v1.json` and `currentPreset.json` were rewritten too. The bundle's principal class, `TestStorageIsolation`, now points `Disk` at a temporary folder before any test runs, and `StorageIsolationTests` checks it. **Any new file a test can reach must go through `Disk`**, or be redirected the same way. ADR-035, ADR-036 |
| **`sips -c` crops around the centre** | With `--cropOffset 0 0` it ignored the offset, so a mockup's panel image silently included the toolbar below it. Crop with PIL. ADR-035 |
| **A build signed by `xcodebuild test` is not the real plugin** | For the test runner, Xcode adds `temporary-exception.files.absolute-path.read-only` `/` and `testmanagerd` lookups. A following `xcodebuild build` with nothing to compile keeps that signature, so installs shipped it, and the plugin read the shared preset folder its real sandbox forbids. The first plain install loaded no presets and crashed on ▶. `Scripts/validate-au.sh` now refuses a plugin carrying `testmanagerd`. **Check `codesign -d --entitlements -` before believing anything about the plugin's sandbox.** ADR-038 |
| **In a sandbox, `fileExists` is not "can I read it"** | The plugin sees that `banks.json` exists and cannot read a byte of it. `Disk.exists` uses `isReadableFile`, so every caller falls back instead of taking a load that fails silently. ADR-038 |
| **A sandboxed plugin hanging at 0% CPU in `_libsecinit_appsandbox` is waiting for a person** | When the plugin's signature changed from ad-hoc to team-signed, `secinitd` found the new identity "not in ACL" for the existing `~/Library/Containers/…AUv3` container and put up a consent dialog, on a display nobody was watching. Every later launch queued behind it on a per-bundle serial queue, so a correct build looked broken. `sample` the per-user `secinitd`: a thread in `displaySharingConsentPrompt:` means ask the owner to look for the dialog. Separately, an App Group on macOS must be Team-ID-prefixed (`8RSH7U3222.…`); `group.…` is rejected without a profile listing it. In the Bash tool's zsh, `log` is a builtin: use `/usr/bin/log`. ADR-041 |
| **The standalone reopens at a remembered size, whatever `willConnectTo` sets** | After ADR-037 it kept reopening at 1024×900. Neither rewriting `NSWindow Frame MainSceneWindow` nor `requestGeometryUpdate` on activation changed that. Capping `sizeRestrictions.maximumSize` at the design size in `configure`, then raising it a second after launch, did. The revert check could not recreate the fault: after one capped launch, even the uncapped build reopened at 800, whether 900 had been set by a resize or by `requestGeometryUpdate`. So the cap is a guard, not a proven fix. **Measure the window with lldb (`UIWindow` and the interface's frame), not from a screenshot**: a screenshot with a sheet open suggested the interface had not followed the window, and it had. ADR-042 |
| **The standalone goes silent when the audio device changes, and the log blames the microphone** | AirPods taken out of an ear moved the default output. AVAudioEngine stopped itself ("iounit configuration changed > stopping the engine") and posted `AVAudioEngineConfigurationChange`, which nothing handled. In the same second tccd logged a refused microphone request under the hardened runtime: CoreAudio rebuilding its aggregate device, not the cause. `S1AudioEngine` now restarts an engine that should be running. **Read the app's own `avae` and `aqme` log lines before chasing TCC.** ADR-043 |
| **Apple's share sheet cannot be presented from the plugin** | `UIActivityViewController` crashed the plugin from both Share buttons. The Mac bridge places the sheet relative to the presenting view's window, and a plugin's view lives in the host's, so `_sceneViewRectFromUIWindowRect` fails an assertion. It is thrown after `present` returns, from UIKit's commit pass, so it cannot be caught at the call. Anything bridged to a Mac panel from the plugin is suspect until tried in a real host. The export `UIDocumentPickerViewController` does present there: confirmed in Logic. ADR-044 |
| **`pkill -f <app path>` also kills an lldb whose command line holds that path** | lldb died with its output unflushed, and the app was left stopped (state `T`) under a dead debugger. Kill the app with `pkill -x ArcadeRuins`, and never SIGTERM a debugged process: lldb stops it and waits. Regex breakpoints set before launch also slow startup past 12 seconds; attach to a running app instead. ADR-042 |
| **`xcodegen generate` rewrites both `Info.plist`s** from `project.yml`'s `info.properties` | A hand edit to a plist lasts one generate; the version keys are `$(MARKETING_VERSION)` / `$(CURRENT_PROJECT_VERSION)` in `project.yml`. P6-8/P7 |
| **A custom-drawn control keeps a stretched bitmap after its bounds change** | UIKit's default `contentMode` scales the old drawing; a stepper rendered as a smear. Every desktop dress sets `contentMode = .redraw`. P6-8 |
| **The render driver shows the owner's library** | Their banks replace factory BankA; public screenshots use `RENDER_SELECT="Brice Beasley,0: "`. `RENDER_ARGS="-S1Skin neonRuins"` picks a skin without touching their defaults (UserDefaults reads `-Key value` arguments). P6-8/P7 |
| **`sendActions(for:)` is inert in the test host** | No `UIApplication` to dispatch through. Invoke an `S1ActionButton.action` closure, or call the layout API. P8-0 |
| **`xcodebuild test` runs the tests inside the app, so `UserDefaults.standard` is the owner's** | P7-5's tests chose a layout and a skin and tidied up after themselves, which wrote and then deleted the owner's real settings — ADR-036's hazard, preferences edition. The layout and skin defaults go through `S1Preferences.store` now, which `TestStorageIsolation` points at a scratch suite. **Any new preference a test can reach must go the same way.** And a render never needs the owner's defaults: pass `RENDER_ARGS="-S1Skin cabinet -S1ClassicLayout NO"`. ADR-050 |
| **In the plugin a parameter write lands at the next render, so a read straight back is stale** | The interface writes through the parameter tree (ADR-022); the kernel holds the old value for up to a block. Upstream reads back after writing in several places. `S1HostedSynth.getSynthParameter` covers it (a write still on its way is the answer), but **a test that drives the plugin's interface must keep rendering while it waits** — `waitWhileRendering` in `HostMIDITests` — or the main thread is the only clock and nothing ever lands. ADR-063 |
| **macOS 26 shapes and lights every app icon** | A legacy `.icns` or `.appiconset` is composited *inside* the system's rounded shape on a light plate — the owner's rounded square came out nested in a white one. An Icon Composer `.icon` bundle is the artwork the system shapes, so it fills the icon. It is also **lit like glass** unless the group says `specular: false` and `translucency: {enabled: false}`, which on dark artwork reads as the lower half fading to white. **Check what macOS draws, not what you shipped**: `NSWorkspace.icon(forFile:)` on the *installed* app, a dozen lines of Swift, settled both in one run each. ADR-052 |
| **The engine must compile with no Apple headers, and `#import` is not `#include`** | Everything under `Sources/S1Engine` is built by GCC and MSVC too. `Tests/Engine/NoAppleHeaders.cmake` fails on `#import`, Foundation/AudioToolbox includes, `NS*`, `AU*` types and Objective-C source (comments excepted). Spell standard headers out — libc++ supplies `<memory>`, `<functional>`, `<cmath>` transitively and the others do not — and use `nullptr`, `bool`, `uint32_t`. ADR-067 |
| **`abs(float)` is `abs(int)` under libstdc++** | Three nearest-rate searches in `S1Rate.hpp` truncated their differences on Linux and picked wrong tempo-synced rates; libc++ and MSVC find a float overload so the Mac never saw it. Write `std::fabs`. The engine's fixtures (goldens, tunings, presets) are what catch this class of thing — read the measured counts in the CI log. ADR-069 |
| **The kernel owns and frees its memory now; upstream's never did** | `~S1DSPKernel` was `= default`, `init()` dropped the note states' Soundpipe modules, nothing freed the 52 oscillator tables: 79.6 MB over eleven kernels under `leaks`. The destructor calls `destroy()`, frees the note states, then the tables (last — the note states borrow them); `destroy()` still leaves the tables alone, because they outlive allocate/deallocate. Anything new the kernel creates needs its line in `destroy()`. Check with `leaks --atExit -- <engine test> <args>`. ADR-070 |
| **`-ffp-contract=on` is part of the sound; the goldens have two readers** | Xcode's Clang fuses `a*b+c` on arm64 by default, so the product and the goldens always had FMA. The CMake engine said `off` until X1-7 measured it: 3 of 20 goldens with `off`, 20 of 20 bit-exact with `on`. `Tests/Engine/GoldenHarness.cpp` renders the twenty through `Sources/S1Engine` alone — exact or fail on Apple Silicon, a measured tolerance on Linux/Windows (one-sample ~0.02 differences there are the bit-crusher's sample-and-hold, not a bug). Rewrite a golden and both readers change; add one and add it to `kGoldens` too. ADR-071 |
| **A smoothed parameter's entry in `parameters[]` is where its glide has got to, not what it was set to** | `prepareToRender` saved `parameters` across `init` (upstream's allocate sequence), so a value set just before allocation came back as the old one for all 45 smoothed parameters: cutoff written 777, read back 20,000. A render test could not see it — plugin and reference lost the values identically; a test that reads the kernel did. Use `getSynthParameter` for "what is it set to". And take a parameter's identifier from the enum, never from `presetKey` (19 differ, one is duplicated). ADR-073 |
| **The kernel's notes and the router's keys must be dropped TOGETHER** | Twice now one was cleared without the other and the same key's next note-on was swallowed (the router does not press a key it believes down): the transport stop (ADR-077) and every re-prepare — an audio device change, a host's sample-rate change (ADR-081). Anything new that silences or resets the kernel's notes calls the router too (`transportDidStop`, `kernelWasPrepared`, `allNotesOff`). |
| **A test patch that never compresses hides the compressors** | Until X2-9 the JUCE plugin started with `s1::Preset()` — ratios clamped to 1:1 — and every plugin test played it, so nothing saw that upstream builds the three compressors in the kernel's CONSTRUCTOR at the constructor's sample rate and never again: 2.2× too fast at 96 kHz, in the Mac AUv3 too. The shipped Init compresses, and `PluginRender` failed the day it became the starting sound. Third time this lesson (no-sustain patches found the transport bug, short effects the denormals): **test with sounds that use the thing.** ADR-080 |
| **pluginval says SUCCESS with Steinberg's validator skipped** | Without `--vst3validator <path>` pluginval 1.0.4 prints `Skipping vst3 validator as validator path hasn't been set` and passes. The validator then found what pluginval had not: a program with no name (536 of 537). Build `validator` from the pinned VST3 SDK and hand it over; the workflow and `Scripts/validate-plugin.sh` fail on the word "Skipping". ADR-079 |
| **There is no CI: three scripts are** | GitHub stopped starting this repository's jobs on 2026-09-18 and the owner is not changing its billing. **Before a merge:** macOS here (`ctest` in `build/plugin`, `Scripts/validate-plugin.sh`, the Xcode suite; `Scripts/build.sh` + `Scripts/validate-au.sh` for engine or Mac sources), **Linux here in Docker** (`Scripts/validate-linux.sh` — GCC build, every test, Steinberg's validator, pluginval, and the RealtimeSanitizer under Clang 20; Docker Desktop must be running; `--x86` for an emulated x86-64 run, slow), and **Windows on the owner's machine** (`Scripts\validate-windows.ps1`; a step whose build failed now reports "not run" rather than testing yesterday's binaries, and the block prints any uncommitted edits; they paste its block back — until then say in STATE.md that Windows is owed for the commit). The workflows stay for the day jobs run again. ADR-079, second amendment |
| **The RealtimeSanitizer stage is the test for "nothing allocates on the audio thread"** | `Scripts/validate-linux.sh rtsan`. Its first run found two upstream allocations in the render call (the voices made lazily in the first `process`; a `std::list` node per arpeggio note), both in the Mac products for years. Anything new on the render path — a container, a `std::function`, a string — gets caught there and nowhere else. ADR-079 |
| **CI is Linux and Windows only; macOS is YOURS to run before a merge** | macOS left both workflows' matrices on 2026-09-18 (its minutes bill ×10 on a private repo and spent the allowance). Before merging anything the CMake side touches: `ctest` in `build/plugin` (goldens bit-exact), `Scripts/validate-plugin.sh`, and the Xcode suite; for engine or Mac-source changes also `Scripts/build.sh` + `Scripts/validate-au.sh`. Never make this repository public to get free minutes — see ADR-079's amendment for what that would publish. ADR-079 |
| **CI that fails in 8 seconds on every job is billing, not YAML** | `gh run view <id>` shows the annotation: "recent account payments have failed or your spending limit needs to be increased". Private-repo minutes: macOS counts ×10, Windows ×2, and a `plugin` run is ~14 min per OS. Nothing in the repository fixes it; it is the owner's, in GitHub's Billing settings. ADR-079 |
| **A timing assertion on a CI machine reads its neighbours** | X2-7's first run measured one second of the plugin's tail at 1.48× the sounding part on Linux — against a 1.5× limit — and the next run 1.01×. Noise only ever ADDS time: perform the thing three times and keep each second's quickest median, then the limit can be tight (1.25×) and mean something. And `gh run view --log` has nothing until the WHOLE run is finished, slowest OS included. ADR-078 |
| **Releasing a voice outside the render loop needs FOUR lines, not two** | P4-5's transport stop set `stage = stageRelease; internalGate = 0` and nothing else. A voice already under the release threshold (any sound with no sustain) is then freed before it runs again, its envelopes never see the gate fall, and the voice's next note has no attack: after a stop the first note of the next phrase was silent — in the Mac AUv3 too. `turnOffKey` also runs `sp_adsr_compute` once on `adsr` and `fadsr` with the gate down; so must anything like it. The stop also left every key down (arpeggio went on; the router swallowed the same key's next note-on) — it is an all-notes-off now. Found by `Tests/Plugin/PluginTransportTests`. ADR-077 |
| **A preset's fields are not its parameter values** | Loading factory presets field by field into the JUCE plugin gave Cool Beans Epic Mega Pad a 0.599 s delay where a new Mac engine plays 0.667 s: upstream's `Preset::apply` writes `delayTime` while the *previous* tempo-sync setting stands, the kernel quantises it to a note value, and the preset's own sync switch arrives later. The order is part of the sound, and so is what was loaded before. Apply a preset to a new kernel and read the 150 values back (`S1PluginState.cpp`); the goldens do the same. ADR-076 |
| **`S1HostMIDI` has three keepers now** | The standalone's Swift chain, the router, and `Tests/Engine/Fixtures/host-midi.txt` (what the standalone played, replayed through the router on Linux and Windows). Change a MIDI rule in one and `HostMIDIParityTests` fails on the Mac, or its check-mode fixture test does, or `HostMIDITests` does in CI — then run `Scripts/write-host-midi-fixtures.sh`. And after a revert-check that restores a source file, rebuild the library target explicitly: `make` kept the mutated object twice when the restore landed in the same second. ADR-075 |
| **Upstream's render depends on the host's buffer size — whole arpeggios, not just tail ends** | Upstream frees released voices once per `process()` call, and `turnOnKey` gives a still-sounding key its old voice back, so a retriggered note's voice depends on where a buffer boundary fell: against the goldens upstream's path is 20/20 exact at 512 frames and 16/20 within tolerance at 64 (relative RMS up to 0.9). First read as "a tail's end" from Init alone (ADR-072) — measure all twenty before naming a cause. `freeReleasedVoicesEveryFrame` (= upstream in 1-frame buffers) is on in the JUCE plugin and off in the Mac products, whose goldens it would rewrite; with it off, compare only renders cut identically. ADR-074 |
| **CMake cannot link with the Command Line Tools SDK here** | `cmake` reports "the C compiler is not able to compile a simple test program"; underneath, `ld: tapi error: malformed file … unknown architecture arm64e.x1-macos` in the CLT's `libSystem.tbd`. `export SDKROOT=$(xcrun --sdk macosx --show-sdk-path)` first. Xcode builds are unaffected. ADR-065 |
| **The JUCE interface's layout is a generated file** | `Sources/S1Plugin/Layout/layout-spec.json` is written by `LayoutSpecFixtureTests` from the Mac desktop layout (frames, bindings, fonts, both palettes). Touch anything under `Sources/SynthOneCore/Desktop` — or a panel's `conductor.bind` — and that test fails until `Scripts/write-layout-spec.sh` has been run; commit the JSON with the change. Six controls are wired by hand and listed in the test (the five dependent-parameter knobs, Transpose): the conductor's bindings do not know them. And **a layout measured in the test bundle is not the app's until checked**: `Scripts/check-layout-spec.sh` compares it with frames read out of the running app. ADR-083 |
| **A label's specification text is not what is drawn** | The status-bar hint carries an Option sign and a non-breaking hyphen; the editor replaces both before painting, because few faces have them. A width test measuring the RAW text passed on macOS and failed on Linux, where a font-less container substitutes something wider. Measure `Style::drawable(text)`. ADR-088 |
| **JUCE clips a component's painting to its bounds; UIKit does not** | The cabinet's joystick leans out of its own box, and on the Mac the sprite simply draws outside the view (`clipsToBounds` is false by default). Here the leaned ball was cut off flush against the console — the owner saw it as "obscured by the yellow buttons". The box is wider than the grab area now (`Joystick::kSwing`), with `hitTest` keeping the clicks where they were. Anything ported from a UIKit view that moves, rotates or glows outside itself has the same trap. ADR-088 |
| **A painted glow is not a layer's shadow** | `CALayer.shadowOpacity` falls BEHIND opaque content; `juce::DropShadow` paints where you put it. The CRT frame's glow tinted the pads it was framing until it was clipped to outside its own rounded rectangle (an even-odd path). Seen by eye first, then made a check. ADR-088 |
| **A check that reads back what it just wrote cannot fail** | X3-6's joystick has a 15% dead zone on the PITCH and not on the picture; the first check asked the stick for its lean — the number the test had just set — and passed with the dead zone wrongly applied to the drawing too. It measures the PIXELS now. Fourth such check in five tasks (ADR-084, ADR-085, ADR-086, ADR-088). |
| **A component answers no clicks until something makes it visible** | `Component::getComponentAt` returns nothing at all for a component whose `visibleFlag` is false, and a freshly constructed one is invisible until a parent adds it or `setVisible(true)` is called. X3-7's hit-test check compared 0 controls with 0 controls at two scales and passed. A test that asks what is under a point must show the editor first, as a host does. ADR-089 |
| **An opaque component's snapshot is opaque whatever you paint** | `createComponentSnapshot` makes an **RGB** image when `isOpaque()`, and RGB has no alpha to be 0 — so "every pixel's alpha is 255" cannot fail. X3-7's letterbox check passed with the fill deleted. Measure the COLOUR (Cabinet's window is `#08070d`, not the black an unpainted image leaves). Sixth such check in seven tasks. ADR-089 |
| **In JUCE, caching does not cost sharpness** | `setBufferedToImage` and component effects render at the graphics context's *physical pixel scale*, which includes every transform above them, so a buffered backdrop is redrawn when the window's scale changes — buffering the whole stage as a revert changed nothing here, and the owner measured the same on Windows (a ratio of 1.0725 with the cache off against 1.0711 with it on). Scaling by painting into a design-size image yourself is what would blur it. ADR-089 |
| **Ask what a control SOUNDS, not where it sits** | X3-8's octave control moved the drawer's keys AND let the router shift the note: every key sounded two octaves up, not one. Every check passed, because they all asked where the keys were. A render showed it. Third time this shape (ADR-085's dependent knobs, ADR-087's tunings): **when a control changes what is heard, measure what is heard.** ADR-090 |
| **`Button::triggerClick` POSTS its callback** | Like `TextEditor::setText` (ADR-086), it goes through the message loop — which a console test does not have, so the click never lands. Call the button's `onClick` yourself. ADR-090 |
| **A comparison whose two sides share the fault cannot see it** | X3-7's first sharpness check read a border at 1.5× and the same border at 1×, and a half-pixel offset on the transform moved BOTH — `0E3540` in each, one level apart, passing. It is the read-back-what-you-wrote trap one step removed: when a mutation moves the baseline too, the difference stays put. Measure the thing ABSOLUTELY (blue 185 on the grid, 62 off it), the way ADR-084 does. ADR-089 |
| **A pixel threshold fitted to one renderer does not hold on another** | X3-7's first crispness check compared a native 1.5× render with a 1× render put through `Image::rescaled`: 1.31× here, **1.07 on Windows**, against a threshold of 1.15 — the owner's run failed on it, and they showed the cache was innocent. Each platform's resampler is its own, so *anything measured against a rescaled image measures the platform*. Compare two things this code draws, or read an absolute colour. ADR-089 |
| **Ask what a SECOND press does, not just the first** | X3-9's power buttons were checked one press at a time from rest, which is the only sequence that works when a cycle assumes every zone starts in the state it is leaving. Pressing "on" once a second held the interface dark for as long as the owner kept pressing, and pressing the other button mid-cycle blacked out the half still lit — neither reachable by a check that presses once. A scratch program that drove the real buttons on the real clock found it in three tries. Anything with a cycle, an animation or a transition gets asked: pressed again, pressed mid-flight, pressed both ways. ADR-099 |
| **NEVER judge a build by grepping its output — read the EXIT STATUS** | `cmake --build … \| grep -E " error"` reported nothing while the link was failing with `make: *** [all] Error 2`: the words do not match, so a broken build looked clean and `ctest` then ran YESTERDAY'S binaries and said 43/43. That is how the caption fix was reported green twice without ever having been linked (2026-09-21). Run the build, capture `$?`, and only then run the tests; a test binary older than the source it tests is the tell (`ls -la` the artefacts). |
| **An Xcode update leaves a CMake build tree pointing at the SDK that is gone** | After Xcode 27 replaced `MacOSX26.5.sdk`, the release build failed with `No rule to make target …/MacOSX26.5.sdk/usr/lib/libm.tbd` — which reads like a broken link and is a stale cache: CMake wrote the absolute SDK path into every link line at configure time. Delete the build tree and configure again; `SDKROOT` alone does not fix it. The same update also blocks `/usr/bin/python3` until `sudo xcodebuild -license` is agreed — the owner's to run. Found 2026-09-21 |
| **This Mac cannot run Intel code, and a Linux binary needs the glibc it was built on** | No Rosetta here (`arch -x86_64`: "Bad CPU type"), so the universal build's Intel half compiles, links and signs and has never been RUN — and the first universal build was also the first time anything compiled for an Intel Mac (`FE_DFL_DISABLE_DENORMS_ENV` is Apple Silicon's name; Intel's has `SSE_` in it). On Linux the release is built on Ubuntu 22.04, not the validation image's 24.04, or nothing of 22.04's age can load it; proved by installing the tarball on a clean Debian 12. And on Windows the release links the C runtime IN, because a plugin that cannot find `VCRUNTIME140.dll` fails silently. ADR-094 |
| **auval re-initialises with NO audio between, and nothing else does** | It sets a parameter, uninitialises, initialises, reads it back — and fails the AU if it moved. The plugin reported the engine's note-value snapping from `prepareToPlay`, silently; pluginval at 10 and Steinberg's validator passed that for a week. Logic will not load an AU that fails auval. Anything the engine changes is reported from the RENDERED BLOCK, never from prepare. `Scripts/validate-plugin.sh --au`; the check is in `PluginParameters` with auval's own numbers. ADR-093 |
| **Read an image's pixels only after its `juce::Graphics` is gone** | On Windows the drawing is not in the image until the Graphics that drew it is destroyed; macOS and Linux write through at once, so a test that draws and reads in one scope passes here and measures **0** there. It failed the owner's run twice (`2d9a5e1`, `f0075c3`) — and their fix from the first run never reached this side, because a fix made on their machine arrives only if it is in the pasted block. Put the Graphics in its own braces, or draw inside a function that returns the image. Found 2026-09-21 |
| **A SafePointer is NEVER made in a capture list** | `[safe = juce::Component::SafePointer<T>(this)]` is banned outright — make it a named local and capture it by copy. Judgement about which ones are nested is what failed: the nested form broke the owner's MSVC build, was fixed by them, and was **reintroduced in the same session, hours after being written into this table**. `ctest -R NoNestedInitCapture` greps for it on every OS, because Clang and GCC accept it and nothing here can. ADR-088 |
| **MSVC refuses an init-capture inside a NESTED lambda** | `[this] { callAsync([safe = juce::Component::SafePointer<T>(this)] { … }); }` compiles under Clang and GCC and not under MSVC, where the inner initialiser cannot reach the outer lambda's `this`. Five of them in `S1PluginEditor.cpp` kept the whole plugin from building on Windows for two tasks, and only the owner's machine had the fix — make the SafePointer a LOCAL and capture it by copy. A single lambda with an init-capture, straight inside a member function, is fine (`S1PresetPanel.cpp` has three). **Nothing here compiles on Windows until the owner runs the script**, so a construct only MSVC dislikes can sit in `main` for weeks: `Scripts\validate-windows.ps1` now prints any uncommitted edits inside its block so a fix made there comes back with the run. Found 2026-09-20 |
| **`frequencyA4` is read by NOTHING in the engine — on the Mac too** | The kernel's own comment says "special case for updating the tuning table based on frequency at A4" and then only truncates the value. X3-5 made it live **in the plugin** (`middleC = A4 × 2^(-9/12)`, TuneUp's arithmetic inverted), which moves five factory presets that ask for 410/432/434/439 and leaves the goldens alone because the engine is untouched. The Mac products still ignore it. ADR-087 |
| **A tuning is measurable as an INTERVAL, never as a pitch, and not with any sound** | A preset may sound an octave below the key it is given, so absolute frequencies measure the preset. And measuring the interval under a microtonal PRESET still failed: "JEC Digiharp" runs two detuned oscillators and autocorrelation reads the blend (292 cents where its scale says 315). Apply the scale to the shipped Init, and check the presets' own scales against the kernel's 128 frequencies instead. Autocorrelation also locks to the octave BELOW — take the shortest tall peak — and a window at the note's start catches the glide. ADR-087 |
| **The plugin's banks are the person's FILES, written on the browser's first use** | `PresetBrowser::load()` writes the twelve banks the Mac starts with into `<app data>/BadPackets/Arcade Ruins/Banks` — what `loadBanks` does on the Mac's first launch — because starring, reordering, renaming and deleting all write to a bank the binary holds read-only. About 2 MB, once, and **only when a person asks for a preset**: the editor makes the browser lazily. What a HOST stores never moves (`factoryProgram` still reads the linked banks). **Anything that can reach the browser must be given a temporary folder first** — the real one is the owner's (ADR-036's rule again; `PluginEditorTests` and `EditorSnapshot` both set one). ADR-086 |
| **The shipped banks hold 670 uids for 695 presets** | 23 are used twice — upstream copied presets between banks and never noticed, because its tables hold object references. Here a uid names a row, a save and a deletion, so reordering Starter Bank moved a preset in another bank. `readFolder` gives the second of each pair a fresh uid and writes that bank back once. Found by a reorder test failing, not by reading the data. ADR-086 |
| **`juce::TextEditor::setText` POSTS its change message** | `postCommandMessage`, so `onTextChange` runs when the message loop next turns — never in a test with no loop. A search field set from code looked inert and the model never heard it. Tell the model yourself. ADR-086 |
| **A tempo-synced knob sits by NOTE VALUE, not by hertz** | The five dependent parameters (LFO 1 / 2 rate, auto-pan rate, delay time, the sequencer's step length) are host parameters in Hz, seconds and bars, but the Mac's knobs and pad 1 sit where the engine's `S1Rate` puts the note value: "1/8 note" is 60% of the travel at any tempo, where the Hz range says 15%. No test saw it — every control painted, moved and wrote its parameter — the side-by-side with the Mac on the same preset did. `s1ui::dependentPositionMap`. **Compare pictures on the same preset before believing a parity claim.** ADR-085 |
| **A check that cannot fail is not a check — and colours six levels apart cannot fail** | X3-2's first "edges stay on the pixel grid" test compared a Studio border (`#101011`) with its well (`#161617`) at a tolerance of 6: it passed with the border drawn half a pixel off. Found only because the revert-check did NOT fail. Measure on a control whose colours differ (Cabinet's cyan border: blue 212 on the grid, 97 off it), and always run the revert. ADR-084 |
| **Storyboard cells keep their autoresized frames** | `PresetCell`'s buttons sat at x 324…487 of a 499-point cell; narrower desktop rows clipped three of them for two versions. Lay out in `layoutSubviews` under the desktop flag. P8-0 |
